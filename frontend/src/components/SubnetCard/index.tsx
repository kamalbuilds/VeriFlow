import React from 'react';

const SubnetCard = ({
    subnet,
    handleOpenInfoModal
}: {
    subnet: any;
    handleOpenInfoModal: () => void
}) => {
    return (
        <div>

        </div>
    );
};

export default SubnetCard;